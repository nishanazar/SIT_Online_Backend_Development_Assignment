# JavaScript Basics — Assignment 06

Arrays & Arrow Functions 

## Topics Covered
- Array `length`
- `toString()`
- `at()`
- `join()`
- `push()`, `pop()`, `shift()`, `unshift()`
- `concat()`, `slice()`, `splice()`
- `delete` operator
- `Array.isArray()`
- Arrow Functions

## Questions

1. **Basic Array Methods** — `fruits` array: length, toString(), at(), join()
2. **Adding & Removing Elements** — `students` array: push(), pop(), unshift(), shift()
3. **concat(), slice(), splice(), delete** — combining, copying, modifying and deleting array elements
4. **Array.isArray() & Arrow Functions** — checking array types, showArray() arrow function
5. **Mini Project: Product List Manager** — combining all the above concepts in one program

## File Structure
```
├── question1.js
├── question2.js
├── question3.js
├── question4.js
├── question5.js
└── README.md
```

Each question has its own JS file 

## Status
✅ All questions completed and tested in browser.